import 'package:flutter/material.dart';

class addHabitQuantitative extends StatelessWidget {
  const addHabitQuantitative({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add Habit"),
      ),
    );
  }
}
